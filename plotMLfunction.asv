n=10000;
startpoint = -1;
finishpoint = 1;
% t = linspace(startpoint,finishpoint,n);
t = startpoint:((finishpoint-startpoint)/n):finishpoint;
beta = 1;
hold on
alpha = 0.5;
x = 0;
y=MLFunction(x,alpha,beta);
% plot(startpoint,MLFunction(startpoint,alpha,beta));
plot(x,y);
alpha = 0.1:0.1:0.9;
for i =1:
%     for i=1:n+1
%         plot(t(i),MLFunction(t(i),alpha, beta), 'k');
% 
%     end
    y = MLFunction(t,alpha(i),beta);
    plot(t,y);

end

% x = 0:pi/100:2*pi;
% y = sin(t);
